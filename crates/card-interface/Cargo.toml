[package]
name = "card-interface"
version = "0.1.0"
edition = "2021"
description = "Shared card backend trait for CCID reader firmware"
license = "GPL-2.0-or-later"

[features]
default = []
std = []

[dependencies]
