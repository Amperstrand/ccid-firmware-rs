[package]
name = "ccid-firmware-rs"
version = "0.0.1"
edition = "2021"
description = "USB CCID firmware for STM32 microcontrollers in Rust"
license = "MIT OR Apache-2.0"
repository = "https://github.com/Amperstrand/ccid-firmware-rs"
readme = "README.md"
keywords = ["ccid", "usb", "smartcard", "stm32", "embedded"]
categories = ["embedded", "hardware-support", "no-std"]

[[bin]]
name = "ccid-firmware"
path = "src/main.rs"
test = false
bench = false

[dependencies]

[target.'cfg(all(target_arch = "arm", target_os = "none"))'.dependencies]
cortex-m = { version = "0.7", features = ["inline-asm"] }
cortex-m-rt = "0.7"
defmt = "0.3"
defmt-rtt = "0.4"
panic-probe = { version = "0.3", features = ["print-defmt"] }
stm32f4xx-hal = { git = "https://github.com/Amperstrand/stm32f4xx-hal.git", features = ["stm32f469", "usb_fs"] }
usb-device = "0.3"
embedded-alloc = "0.5"

[patch.crates-io]
synopsys-usb-otg = { path = "vendor/synopsys-usb-otg-f469" }

[profile.release]
codegen-units = 1
debug = true
lto = true
